


<h1>All Recipes</h1>
<div class="row">
  <div class="col-sm-6 col-md-3">
    <div class="thumbnail">
      <div class="caption">
          <% @recipe.each do |recipe|%>
            <h3><%= link_to recipe.title, recipe_path(recipe.id)%></h3>
            <% end %>
      </div>
    </div>
  </div>
</div>

  <h3> Add Recipes </h3>
  <%= link_to "New", "new", class:"btn btn-default" %>
